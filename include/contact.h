#include <string>
#include <iostream>

class Contact{
public:
  Contact(string firstName, string lastName, string emailAddress) {
    firstName = fn;
    lastName = ln;
    emailAddress = em;
  }


private:
  string firstName, lastName, emailAddress;

};
